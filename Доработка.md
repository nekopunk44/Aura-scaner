# Aura Scanner Progress Notes

Updated: 2026-06-16

## Completed Recently

- Hardened backend social login to bind accounts by provider identity
- Stopped leaking auth tokens through callback URLs by using exchange flows
- Locked Telegram auth behind a real bot token requirement
- Aligned OAuth redirect config with `com.aurascanner.app`
- Cleaned branding and metadata in `scanner_ap`
- Reworked the `signature` app so signatures can be saved, replaced, and deleted locally
- Added backend tests for OAuth link generation and Telegram auth validation

## Verified

- `backend`: `npm test`, `npm run build`
- `scanner_ap`: `flutter analyze`, targeted auth config tests
- `signature`: `flutter analyze`

## Next Priorities

1. Add automated tests for `signature`
2. Run end-to-end mobile checks for auth, document sync, and deep links
3. Replace placeholder production credentials with real provider values
4. Decide whether the standalone `signature` app should remain separate from the embedded signature flow in `scanner_ap`
5. Add broader integration coverage for document sync and social auth edge cases

## Notes

- The internal Dart package name in the main app is still `scanner_ap`
- Desktop binary names still use template-style internal identifiers in a few platform files
- Production release signing and store submission assets are still outside the repository
